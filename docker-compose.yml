name: test-hc-frontend-container
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: test-hc-frontend-container
    restart: always
    ports:
      - "8082:80"
    environment:
      - NODE_ENV=production
